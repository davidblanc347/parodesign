# OpenAI API Configuration (NEXT_PUBLIC_ prefix required for client-side access)
NEXT_PUBLIC_OPENAI_API_KEY=your_openai_api_key_here
OPENAI_API_KEY=your_openai_api_key_here

# Optional: Custom WebSocket Relay URL (if not using the built-in relay)
# NEXT_PUBLIC_WS_URL=ws://localhost:3000/api/realtime
